#ifndef SORT_H
#define SORT_H

#include<bits/stdc++.h>
using namespace std;

#include "ProductMain.h"

class Sort
{
    public:
        static bool compare_price(ProductMain ,ProductMain);
        static bool compare_Name(ProductMain, ProductMain);
};

#endif